After finish recreate all samples for EXIT and LLMLonglingua, make results/ dir to save experiments down to file.
Then write shell/py scripts to run all of them one by one for me, 3 times, calculate the avg too. Only need to copy paste is enough.

Model name for llama-3.3 70B: RedHatAI/Llama-3.3-70B-Instruct-FP8-dynamic   (port: 8004)
server friendly hostname: a6k.jrusd.org

Note this: `LBenchEval/` is our custom evaluation pipeline and `LongBench/` is the upstream (original) reference repo. Treat it (`LongBench/`) as read-only reference code.